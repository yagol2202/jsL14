let contador = parseInt(prompt("Digite um número para o contador:"));
let final = parseInt(prompt("Digite o valor do numero final:"));
while (contador <= final) {
    console.log(contador);
    contador++;
}
alert("Contagem finalizada!");